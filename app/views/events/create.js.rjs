if @event.new_record?
  page.replace_html 'event_form', :partial => 'form'
else
  page.call 'showNotice', 'Event successfully created.'
  page.call 'insertEvent', @event.id, render(@event), @event.starts_at.strftime("%m-%d-%Y")
end
